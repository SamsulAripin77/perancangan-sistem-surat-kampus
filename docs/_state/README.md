# Legacy State

This folder is no longer used for active task state.

Use `.ai-context/PROJECT-STATE.json`, `.ai-context/WORK-QUEUE.md`, and
`.ai-context/CURRENT-TASK.md` through the `acode` workflow instead.
